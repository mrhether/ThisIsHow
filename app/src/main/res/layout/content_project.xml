<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="fill_vertical"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".ui.activites.ProjectActivity"
    tools:showIn="@layout/activity_project">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="@dimen/content_divider">

        <com.markhetherington.thisishow.ui.widgets.BigStartTextView
            android:id="@+id/project_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="@dimen/default_margin"/>

        <TextView
            style="@style/TextAppearance.Subtitle"
            android:layout_marginTop="@dimen/content_divider"
            android:text="@string/suplies_and_tools"/>

        <android.support.v7.widget.RecyclerView
            android:id="@+id/material_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <TextView
            style="@style/TextAppearance.Subtitle"
            android:layout_marginTop="@dimen/content_divider"
            android:text="@string/instructions_header"/>

        <android.support.v7.widget.RecyclerView
            android:id="@+id/instruction_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    </LinearLayout>
</android.support.v4.widget.NestedScrollView>
